function calc(fruit){

    if(fruit=== "banana" || "kiwi" || "cherry" || "lemon" || "grapes" || "peach"){
        console.log("fruit")
    }else if(fruit === "tomato" || "cucumber" || "pepper" || "onion" || "garlic" || "parsley"){
       console.log("vegetable")
    }else{
        console.log("unknown")
    }
}

calc("tomato");