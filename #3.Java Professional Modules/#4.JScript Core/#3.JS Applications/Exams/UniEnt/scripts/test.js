function f() {

   /* let request = new CustomRequester()
        .type(RequestType.GET)
        .url(DatabaseURLs.USER_INFO)
        .header(RequestHeaderType.AUTHORIZATION, AuthenticationTypes.BASIC + ' ' + DatabaseConstants.BTOA_TOKEN)
        .data({})
        .run(done, fail);

    console.log();

    function done() {
       console.log('DONE!');
    }

    function fail(error) {
        console.log('FAIL');
        console.log(error.responseJSON.description);
    }*/
}

f();