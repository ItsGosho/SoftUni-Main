const RoutingURLs = {
    HOME: '/',
    USER: {
        LOGIN: '/login',
        REGISTER: '/register',
        LOGOUT: '/logout',
    },
};

const UserRoutingURLs = RoutingURLs.USER;

export {
    RoutingURLs,
    UserRoutingURLs
}