import Actions from "../../constants/actions.constants";

let testInit = {kura: false};

let testReducers = (state = testInit, action) => {

    switch (action.type) {
        default:
            return state;
    }
};

export {
    testReducers
}