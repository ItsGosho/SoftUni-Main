const USER = {
    LOGIN: '/auth/login',
    REGISTER: '/auth/register',
    LOGOUT: '/auth/logout',
};

const MOVIE = {
    CREATE: '/movies/create',
    ALL: '/movies/all'
};

export default {
    USER,
    MOVIE
};